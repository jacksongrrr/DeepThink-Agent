Metadata-Version: 2.4
Name: deepthink-agent
Version: 0.1.0
Summary: 对比「思考路径预处理 + R1」与「纯 R1」的本地 Web Agent
Author: DeepThink Agent Contributors
License: MIT
Project-URL: Homepage, https://github.com/your-org/deepthink-agent
Project-URL: Repository, https://github.com/your-org/deepthink-agent
Keywords: deepseek,reasoning,agent,prompt-engineering
Classifier: Development Status :: 4 - Beta
Classifier: Framework :: FastAPI
Classifier: Intended Audience :: Developers
Classifier: Programming Language :: Python :: 3
Classifier: Programming Language :: Python :: 3.11
Classifier: Programming Language :: Python :: 3.12
Classifier: Typing :: Typed
Requires-Python: >=3.11
Description-Content-Type: text/markdown
License-File: LICENSE
Requires-Dist: fastapi>=0.115.0
Requires-Dist: uvicorn[standard]>=0.32.0
Requires-Dist: openai>=1.59.0
Requires-Dist: pydantic>=2.10.0
Requires-Dist: pydantic-settings>=2.7.0
Requires-Dist: httpx>=0.28.0
Provides-Extra: dev
Requires-Dist: pytest>=8.3.0; extra == "dev"
Requires-Dist: pytest-asyncio>=0.25.0; extra == "dev"
Requires-Dist: ruff>=0.8.0; extra == "dev"
Dynamic: license-file

# DeepThink Agent

在 **DeepSeek R1**（`deepseek-reasoner`）深度推理之前，先用 **Chat**（`deepseek-chat`）生成极简「思考路径 + 每条路径一句原因」，再作为上下文交给 R1。本仓库提供本地 Web 界面，支持：

- **对比**：并行调用「纯 R1」与「思考路径 + R1」，并排展示结果  
- **仅技术**：只跑「思考路径 + R1」  
- **仅基线**：只跑「纯 R1」  

## 快速开始

### 1. 准备环境

```bash
cd 深度-深度思考
python -m venv .venv
.venv\Scripts\activate
pip install -U pip
pip install -e .
```

### 2. 配置密钥

复制 `.env.example` 为 `.env`，填入你的 [DeepSeek API Key](https://platform.deepseek.com)：

```env
DEEPSEEK_API_KEY=sk-你的密钥
```

### 3. 启动

```bash
python main.py
```

浏览器将自动打开（若系统允许）`http://127.0.0.1:8765/`。在页面选择模式并输入问题即可。

> 若未自动打开，请手动访问终端中打印的地址。

## 开发

```bash
pip install -e ".[dev]"
ruff check src tests
ruff format src tests
pytest
```

## 提示词设计要点

- **角色分离**：路径生成、R1 基线、R1+路径 使用独立 `system` 提示，避免指令混写导致权重不清。  
- **分隔符**：用户问题与候选路径分别包在 `<user_question>`、`<candidate_thinking_paths>` 中，降低串扰与注入面。  
- **可解析输出**：Chat 阶段使用 `response_format: json_object`，并在 system 中写死 JSON 契约（`paths[].path` / `paths[].reason`）。  
- **R1 参数**：不对 `deepseek-reasoner` 传入其不支持的采样参数（如 `temperature`）。  

详见 `src/deepthink_agent/prompts.py` 与 `docs/ARCHITECTURE.md`。

## 目录说明

| 路径 | 说明 |
|------|------|
| `main.py` | 启动 Uvicorn 与本地站点 |
| `src/deepthink_agent/` | 配置、提示词、API 客户端、业务编排、FastAPI |
| `static/` | 前端静态资源 |
| `tests/` | 单元测试 |
| `docs/` | 愿景与架构文档 |

## 许可证

MIT，见 `LICENSE`。

## 自检清单（维护者）

- [x] 无核心业务 TODO / 空实现  
- [x] README 可从零跑通主流程（需有效 API Key）  
- [x] `.env.example` 完整  
- [x] 测试可执行（健康检查 + 提示词/解析契约）  
- [x] CI：Ruff + Pytest  
- [x] 文档与代码结构一致（`docs/ARCHITECTURE.md`）  
